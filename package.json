{
  "name": "preact-redux-example",
  "version": "1.0.0",
  "description": "Ready-to-go Preact + Redux starter project, powered by Poi.",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "poi build",
    "serve": "poi build && serve",
    "dev": "poi",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "keywords": [
    "preact",
    "redux"
  ],
  "license": "MIT",
  "author": "Jason Miller <jason@developit.ca>",
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "less": "^2.7.3",
    "less-loader": "^4.0.5",
    "poi": "^9.5.4"
  },
  "dependencies": {
    "preact": "^8.1.0",
    "preact-redux": "^1.2.0",
    "redux": "^3.6.0",
    "serve": "^6.4.1"
  },
  "poi": {
    "entry": "src/index.js",
    "jsx": "h"
  }
}
